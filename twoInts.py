"""
These are stupid functions...don't do this.
"""


def addTwoInts(firstInt_inline: int, secondInt_inline: int) -> int:
    """Stupid addition example

    Args:
        first (int): the first integer
        second (int): the second integer

    Returns:
        [int]: first plus second
    """
    return (firstInt_inline + secondInt_inline)
